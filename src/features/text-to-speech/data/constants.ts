// Matches the cap enforced inside the Chatterbox Space. Raising this means
// raising it in the Space's app.py too, and it burns ZeroGPU quota faster.
export const TEXT_MAX_LENGTH = 300;
export const COST_PER_UNIT = 0.0003;
